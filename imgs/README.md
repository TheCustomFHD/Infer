# imgs

`example.jpg` is linked from the top-level README.

A 1x1 placeholder is committed so the README renders. **Replace it with
a real screenshot** — for example `infer chat` mid-conversation on the
target hardware.
